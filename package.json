{
  "name": "open-teacher-assessment",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && terser app.src.js -o dist/app.js -m -c && html-minifier-terser index.html -o dist/index.html --collapse-whitespace --remove-comments --minify-css true && cp dompurify.min.js favicon.svg og-image.webp og-image.png dist/"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.48.0"
  }
}
